class Solution {
public:
    char findTheDifference(string s, string t) {
        int count = int(t[0]);
        for (int i=1;i<t.size()+1;i++){
            count = count ^ int(s[i-1])^int(t[i]);
        }
        return char(count);
        
    }
};
