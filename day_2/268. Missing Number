class Solution {
public:
    int missingNumber(vector<int>& nums) {
        int count=0;
        for (int i=1; i<nums.size()+1;i++){
            count = count^i^nums[i-1];
        } return count;
    }
};
